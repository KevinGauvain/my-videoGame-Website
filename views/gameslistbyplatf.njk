{% extends "layout.njk" %}
{% block content %}
  <div>
    <h1 class="titre2"> Hello World this is the title from gameslistbyplatf.njk</h1>
    <p>
      {% for game in list %}
      {# <li> {{ game.name }} </li> #}
      <p></p>
      <a href="/gameInformation/{{game.id}}">{{ game.name }}</a>
      {% endfor %}
    </p>
    <p>{{total}}</p>
    <button>
    <a href ="/gameList/:platformId">Page précédente</a>
    </button>
     <button>
    <a href ="http://localhost:3000/gameList/:{{platformId.platformId}}">Page suivante</a>
    </button>
  </div>
{% endblock %}
