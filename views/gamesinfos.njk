{% extends "layout.njk" %}
{% block content %}
  <div>
    <h1 class="titre2"> Hello World this is the title from gamesinfos.njk</h1>
    <p>
      {# {% for game in list %} #}
      <li> {{ list.category  }} </li>
      {# <li> {{ list.cover.url }} </li> #}
      <li><p> Cover : <img src="{{ list.cover.url }}"> </p></li>
      {# <li> {{ list.screenshots[0].url }} </li> #}
       <li><p> Screenshots : <img src="{{ list.screenshots[0].url }}"> </p></li>

      {# {% for screenshots in list %}
        <li> {{ list.screenshots[i].url  }} </li>
      <p></p>
      {% endfor %} #}

    </p>
    <p>{{total}}</p>
  </div>
{% endblock %}

{# A essayer : {{ list.screenshots[0].url  }} + img src #}
